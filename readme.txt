Android sensor simulation enables the user to track the present activity such as walking, running, still, driving by leveraging the mobile sensors and track the information using the APIs by Google for the google maps. Each activity has been customized by changing the UI of the screen. The user has the option to swipe right and see the record of activities performed in the last application run using the SQLite database.